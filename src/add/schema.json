{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsMyComponent",
  "type": "object",
  "properties": {
    "answer": {
      "type": "boolean",
      "description": "需要生成答案吗?",
      "x-prompt": "需要生成答案吗?"
    },
    "analyse": {
      "type": "boolean",
      "description": "需要生成解析吗?",
      "x-prompt": "需要生成解析吗?"
    }
  },
  "required": ["answer", "analyse"]
}
